# Core pipeline dependencies.
#
# These packages are enough for UCDDB download, data audit, epoch/feature
# construction, classical ML training, temporal ensemble evaluation,
# interpretability tables, and final report artifacts.
numpy
pandas>=2.2,<3
scipy
scikit-learn
matplotlib
pyedflib
tqdm
requests
xlrd>=2.0.1
xgboost

# Kept for compatibility with common PhysioNet workflows and future signal IO.
wfdb
